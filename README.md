# agent-zero-cortex

Agent Zero plugin for Cortex memory backend. Runs side-by-side with the existing FAISS plugin.

## Install

Clone this repo and register in Agent Zero's plugin directory.

## Configuration

Set env vars: `CORTEX_URL`, `CORTEX_API_KEY`.

See `default_config.yaml` for all options.
